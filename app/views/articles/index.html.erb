<h1>Articles</h1>

<ul>
    <% @articles.each do |article| %>
        <li>
            <%# link_to renders a link with its first argument as the link's text and its second argument as the link's destination %>
            <%# if we pass a model object as the second argument, link_to will call the appropriate path helper to convert the object to a path %>
            <%= link_to article.title, article %>    
        </li>
    <% end %>
</ul>

<%= link_to "New Article", new_article_path %>
